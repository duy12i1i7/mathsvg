[package]
name = "mathsvg-bench"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Thin launcher for the canonical MathSVG Python benchmark runner"

[lib]
path = "src/lib.rs"

[[bin]]
name = "mathsvg-bench"
path = "src/main.rs"

[lints.rust]
unsafe_code = "forbid"
